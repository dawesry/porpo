{
  "name": "porpo",
  "author": "Ryan Dawes",
  "scripts": {
    "css-watch":"sass --watch --no-source-map _sass/:css/",
    "js-watch": "babel  --extensions '.tsx' _javascript --watch --out-file lib/main.js",
    "dev":"npm-run-all --parallel css-watch js-watch",

    "css-build": "sass --no-source-map _sass/:css/",
    "js-build": "babel --extensions '.tsx' _javascript --out-file lib/main.js",
    "build": "npm-run-all --parallel css-build js-build",

    "copy": "rm -rf dist && mkdir dist && cp CNAME *.html ./dist && cp -r css/ lib/ assets/ ./dist",
    "pages": "gh-pages -d ./dist",
    "clean": "rm -rf dist",
    "deploy": "npm-run-all --sequential build copy pages clean"
  },
  "dependencies": {
    "@babel/cli": "latest",
    "@babel/core": "latest",
    "@babel/preset-typescript": "latest",
    "gh-pages": "^4.0.0",
    "npm-run-all": "^4.1.5",
    "sass": "latest",
    "typescript": "latest",
    "bulma": "latest",
    "react": "latest",
    "react-dom": "latest",
    "@emotion/react": "latest",
    "@emotion/styled": "latest",
    "@mui/icons-material": "latest",
    "@mui/material": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest"
  },
  "devDependencies": {
    "npm": "latest",
    "react-scripts": "latest"
  }
}
