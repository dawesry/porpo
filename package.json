{
  "name": "porpo",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "predeploy": "rm -rf dist && parcel build src/index.html --public-url testing.dawes.cc && cp CNAME ./dist",
    "deploy": "gh-pages -d dist",
    "push-page": "rm -rf dist && parcel build src/index.html --public-url testing.dawes.cc && cp CNAME ./dist && gh-pages -d dist"
  },
  "dependencies": {
    "@emotion/react": "latest",
    "@emotion/styled": "latest",
    "@mui/icons-material": "latest",
    "@mui/material": "latest",
    "@parcel/transformer-sass": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "autoprefixer": "latest",
    "babel-cli": "latest",
    "babel-preset-env": "latest",
    "babel-preset-es2015-ie": "latest",
    "bulma": "latest",
    "gh-pages": "^4.0.0",
    "parcel": "^2.6.2",
    "react": "latest",
    "react-dom": "latest",
    "sass": "latest",
    "typescript": "latest"
  },
  "devDependencies": {
    "process": "^0.11.10",
    "react-scripts": "latest"
  }
}
